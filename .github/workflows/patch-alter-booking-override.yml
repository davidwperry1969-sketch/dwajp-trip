name: Patch Alter Trip booking override
on:
  push:
    branches: [feature/alter-trip-booking-override]
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/alter-trip-booking-override
      - name: Add deliberate booking override workflow
        run: |
          python - <<'PY'